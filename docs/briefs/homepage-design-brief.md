# Homepage Design Brief

| Field | Value |
| --- | --- |
| Document type | Derived working input, not a specification |
| Date | 23 August 2026 |
| Covers | Issues #13, #14, #15 |
| Consumed by | A visual design pass, before any of those issues is implemented |

**This document owns nothing.** It restates decisions that already live in
[PRD.md](../PRD.md) ch. 6 and [DESIGN-SYSTEM.md](../DESIGN-SYSTEM.md) ch. 2 to 7,
assembled into the order a design tool needs to read them. Where this file and
those disagree, **those win and this one is wrong**. Do not edit this file to
change a rule. Edit the owner, then regenerate this.

Its life ends when the design pass does. What the pass decides that the
specifications do not yet cover, chiefly the header and footer, goes back into
DESIGN-SYSTEM.md as new chapters. This file is not the record of that.

All copy inside the design is English, matching the product.

---

## 1. The brand in three paragraphs

iNi ViE Hospitality is a Bali based hospitality group. Not one hotel: six lines
of business across one island. Resorts and villas, Wonderspace for restaurants
and beach clubs, Svaha Wellness for spas, Souljourn, the WeInivie membership,
and a B2B consultancy. **The homepage is a door to six worlds, not a page
selling one property.** Every section is one of those doors.

Its tagline is *"Turn Bali Into Yours."* That is possessive and intimate, and it
is the opposite of how luxury hospitality usually speaks. Four of the company's
eight published mantras are about local people, local culture, and the
environment rather than about luxury. This brand sells closeness, not distance.
Any reference that sells distance is being read for its layout only.

Three mantras translate directly into design constraints:

| Mantra | Constraint |
| --- | --- |
| *"Technology is a tool, not the hero"* | No carousel, no parallax, no showy motion. Calm and fast. |
| *"Meaningful story telling"* | Section order carries a narrative. Spacing rhythm is its visual form. |
| *"Every guest is seen, heard, and remembered"* | One primary action per section. Never five offers at once. |

---

## 2. The locked design system

Colour and type are final, measured against WCAG AA, and enforced by
`web/src/design/palette.test.ts`, which parses `web/src/app/globals.css` on every
test run. **Do not propose alternative palettes.** If a reference tempts a change
here, the reference is wrong.

### 2.1 Colour

| Token | Hex | Use |
| --- | --- | --- |
| `ink` | `#1C2434` | Primary text, dark panels, footer |
| `ink-muted` | `#4A5468` | Secondary text |
| `accent` | `#FF8737` | Button fills and markers only. Never text on a light surface |
| `accent-hover` | `#F46100` | Hover and pressed |
| `gold` | `#C9A779` | Rules, dividers, markers. Never text on a light surface |
| `gold-dark` | `#8E6A39` | The only gold that may be text on a light surface. Used for eyebrows |
| `surface` | `#FFFFFF` | Card backgrounds |
| `surface-alt` | `#F7F7F5` | Alternating section backgrounds |
| `border` | `#E4E6EA` | Separators only, never text |
| `muted` | `#AAB1BB` | Placeholder and disabled only. Never carries meaning |
| `on-accent` | `#1C2434` | Text on an accent fill. This is `ink`, not white |

White on `accent` measures 2.39 to 1 and fails AA, which is why `on-accent`
resolves to ink at 6.49 to 1. Gold on a light surface measures 2.26 to 1, which
is why eyebrows use `gold-dark` at 4.92 to 1. Gold is legible as text only on a
dark panel, at 6.87 to 1.

**Accent discipline.** Orange appears on the primary button, the active
navigation item, and small markers such as a rating star. It is never a section
background, a large fill, or a decorative shape. When a section needs emphasis it
uses an `ink` dark panel, not orange.

### 2.2 Type

Poppins Medium for headings, Inter for body and UI. Both self hosted through
`next/font/google`.

| Role | Mobile | Desktop |
| --- | --- | --- |
| H1, hero | 34px / 40 | 46px / 52 |
| H2, section | 28px / 36 | 34px / 42 |
| H3, card title | 18px / 26 | 20px / 28 |
| Body | 15px / 24 | 16px / 26 |
| Small, labels | 13px / 18 | 13px / 18 |
| Eyebrow | 12px / 16, uppercase, 0.08em tracking | same |

Paragraphs cap at roughly 65 characters. The cap sits on the text container, not
the section, so a section stays full width while its copy stays readable.

### 2.3 Spacing, radius, elevation, motion

| Context | Mobile | Desktop |
| --- | --- | --- |
| Section vertical padding | 64px | 96px |
| Container max width | - | 1280px |
| Container side padding | 20px | 40px |
| Grid gap between cards | 20px | 32px |
| Heading to body | 12px | 16px |
| Section header to content | 32px | 48px |

Radius: cards and card images 12px, buttons and inputs 8px, full round for
avatars and icon buttons only.

Elevation: two levels. A soft low opacity shadow at rest, slightly larger and
darker on card hover. No heavy or coloured shadows. Depth comes from imagery and
spacing.

Motion: 200ms, ease-out, on hover, focus, colour and transform. Scroll entrance
is a fade plus an upward translate of at most 12px. All of it disabled under
`prefers-reduced-motion: reduce`.

---

## 3. Artboards to produce

| # | Artboard | Width | Notes |
| --- | --- | --- | --- |
| 1 | Homepage, full length | 1440px | All 12 sections in order |
| 2 | Homepage, full length | 375px | The same page, mobile first rules applied |
| 3 | Header, transparent state | 1440px | Floating over the hero image |
| 4 | Header, solid state | 1440px | After scroll |
| 5 | Mobile drawer, open | 375px | With the close control and focus order |
| 6 | Property card, three variants | 400px each | Full, no rating, no price with inert CTA |
| 7 | Footer | 1440px and 375px | Four columns and one column |

Reference devices: iPhone SE 375px, iPad Mini 768px, laptop 1440px.

---

## 4. Section by section

The order below is fixed by PRD ch. 6.1. Content marked **real** is taken from
production or from the committed seed data and must be used verbatim rather than
replaced with placeholder text.

### 4.1 Header and navigation

Sticky. Transparent over the hero, solid on scroll, drawer below 1024px.

Navigation is grouped by guest intent, not by company structure. Six items, no
more, and no mega menu:

    Stay · Dine · Wellness · Offers · Membership · About

Right of the nav, one primary button: **Book Now**.

Production runs twelve paths in its header: seven nested primary items plus five
secondary ones. That is the single largest thing this redesign fixes. The B2B
consultancy lines, Consultant, DP+ and DP Construction, move to the footer. Blog,
Mantras, Sustainability and Why Choose Us sit under About.

The transparent state needs a treatment that keeps white text legible over a
photograph. A thin top down gradient scrim, not a full dark overlay that kills
the image. The active item is marked with an accent rule or underline while its
label stays ink, because accent is never text.

The drawer needs a visible close control, touch targets at least 44 by 44 pixels
on both axes, and the same item order as desktop.

### 4.2 Hero and the welcome block

**No carousel**, so the largest contentful paint stays cheap. Height 70vh
mobile, 75vh tablet, 85vh desktop.

Production splits this into two parts, and the split is worth keeping:

**The hero itself** is one large image carrying the search panel and nothing
else. No headline sits on the photograph.

**The welcome block** follows it on a light ground: the H1 *"iNi ViE
Hospitality"* centred, then the company paragraph centred beneath it, then a
single primary CTA.

Keeping them separate solves three things at once. The paragraph is roughly 350
characters, which is unreadable over a photograph but fine on a plain ground.
The hero stays a single image with no text to composite, which is the cheapest
possible largest paint. And the measure cap in ch. 2.2 can actually be honoured,
because a centred block can be constrained to 65 characters without fighting the
image behind it.

Photography leads in the hero. Copy leads in the welcome block. Neither competes
with the other.

### 4.2.1 Search panel

Production carries a destination selector, a check in and check out range, and a
Search button, sitting directly under the navigation and over the top of the
hero image. It is part of the homepage and is designed here.

| Element | Treatment |
| --- | --- |
| Ground | `ink` panel, 12px radius |
| Field labels | Eyebrow scale, `gold` on the ink ground |
| Field values | Body scale, `surface` |
| Search button | Primary, `accent` fill with `ink` text per ch. 2.1 |
| Mobile | Collapses to a single tappable summary row that opens the full panel. Three fields side by side do not fit 375px |

Every control is at least 44 by 44 pixels on both axes. The panel is a form, so
each field needs a visible focus ring per ch. 2.3.

This is the one homepage element outside the twelve sections in PRD ch. 6.1.
Booking itself still runs on a separate system and stays out of scope: the
button leads there, it does not implement it.

### 4.3 Featured Properties, the dynamic section

**This is what the technical test is actually assessing.** It renders from the
Laravel API, not from static content.

Composition: eyebrow *"Stay With Us"*, heading *"Featured property for you"*, a
one to two sentence intro, the card grid, then a secondary **"View All Family"**
control. On desktop that control may sit right aligned on the heading row. On
mobile it moves below the content, because a right aligned link beside a wrapped
heading looks broken at narrow widths.

**On the label.** PRD ch. 6.2 writes this link as "View all properties".
Production says *"View All Family"*, rendered as a filled `ink` pill rather than
a text link, and that is the wording adopted here. It is brand voice: the
portfolio is the family. It is also less immediately obvious to a first time
visitor than "properties" would be, and that cost is accepted knowingly. PRD ch.
6.2 needs updating to match, so the specification and the design do not
disagree.

The API returns three properties by default. **Real** seed data, in sort order:

| Title | Category | Location | Rating | From, IDR per night |
| --- | --- | --- | --- | --- |
| Leedon Villa Seminyak | Villa | Seminyak, Bali | 4.8 | 3,200,000 |
| Ajowa Resort | Resort | Nusa Dua, Bali | 4.6 | 2,400,000 |
| La Mewali Resort | Resort | Uluwatu, Bali | 4.9 | 4,100,000 |

Real excerpts, clamped to three lines:

- *"A walled garden villa two streets back from Petitenget beach, with a private pool and a full kitchen."*
- *"Sixty rooms around a central lagoon pool, a short walk from the calm water of the southern reef."*
- *"Cliffside suites above the break at Bingin, with an open air restaurant facing the sunset."*

Card treatment, per DESIGN-SYSTEM ch. 6.1:

| Element | Treatment |
| --- | --- |
| Image | 4:3, `object-cover`, 12px radius, scales to 1.04 on card hover with overflow clipped |
| Category badge | Top left over the image, `surface` at high opacity, `ink` text, small label |
| Rating | Top right of the content area, a `gold` star and one decimal, small size |
| Title | H3, `ink`, clamped to 2 lines, never truncated mid word |
| Location | Small, `ink-muted`, preceded by a 16px pin icon |
| Description | Body, `ink-muted`, clamped to 3 lines |
| Price | Prefixed with **"From"**, amount in `ink` medium, the "per night" qualifier in `ink-muted`. Reads *"From IDR 3,200,000 per night"* |
| Button | Full width on mobile, auto on desktop, `accent` fill with `ink` text |
| Card | `surface`, 1px `border`, 12px radius, rest elevation, raised on hover |

**Equal height is a rule.** Cards in a row match height regardless of content
length, achieved by clamping title and description and pinning the button to the
bottom, never by fixing a card height. A card whose price row is omitted is the
case this rule exists for: it must end level with its neighbours, not 50 pixels
short of them.

**Why this card carries more than production's.** Production's card carries five
things and no more: a portrait image, a category badge, a status badge reading
*"New Opening"*, the title, and the location. Both badges sit below the image as
outlined pills rather than overlaid on it. There is no price, no rating, no
description and no button.

That is a defensible choice, and the reason behind it is real: a nightly rate
moves with season, length of stay and channel, so a figure typed into a CMS goes
stale and becomes a promise the booking engine will not honour.

This project keeps price and rating anyway, for two reasons. The column is
`price_from`, an indicative floor rather than a live rate, which is the field
hospitality actually publishes and the slowest moving number on the page. And
these two fields are the only ones on the card that can be null, so they are
what demonstrates rule D7 and requirement F5: the price row vanishing whole, the
rating never rendering as zero, the CTA going inert rather than broken. Remove
them and the dynamic section loses the behaviour it exists to prove.

The honesty problem is solved by the word **"From"** rather than by deleting the
field, and an editor who does not want to publish a figure at all simply clears
it, which is already a supported state.

**Worth considering, not yet specified.** Production's *"New Opening"* status
badge is more durable than any price and the schema has nothing like it. Adding
it means a new column, a PRD change, and a matching field in `PropertyResource`
and `types/property.ts` in the same commit. Out of scope for this design pass,
but worth recording as the strongest candidate for the next one.

Three card variants must be drawn, because all three occur in the real data:

1. **Full.** Rating and price both present. Leedon Villa Seminyak.
2. **No rating.** The rating is omitted entirely, never rendered as zero or as
   an empty star.
3. **No price, inert CTA.** The whole price row is omitted, and the button
   renders visually muted and non interactive rather than linking nowhere. Svaha
   Retreat Ubud is the real row with both fields null.

Four more states belong to this section and are worth one small artboard each or
a note on artboard 1:

- **Loading.** Three skeleton cards whose dimensions match the real card
  exactly, image ratio and clamped line counts included. A skeleton with
  different dimensions causes the layout shift it exists to prevent.
- **Six cards.** The grid must tolerate up to six without layout damage.
- **Empty.** When no published properties exist the whole section disappears,
  heading included, leaving no gap.
- **API unreachable.** A quiet fallback, with the rest of the homepage still
  rendering normally.

### 4.4 The Culinary Journey

Three restaurant cards, static content. **Real** venues from production: Norii
Seminyak (Japanese), Riserva Steakhouse (Ubud), Terra Verte (Mediterranean).
Production's line for this section is *"Opening a new chapter in refined dining
experience."*

Visually lighter than the property card. These are not bookable objects on this
page, so they do not need a price row or a rating.

### 4.5 Wellness Harmony Escape

Three spa cards. **Real** venues: Svaha Spa Ajowa, Svaha Spa La Mewali, Svaha
Spa Bisma. Production's line is *"Find serenity in soulful rituals made to
restore."*

Same card weight as Culinary, so the two sections read as a pair rather than as
two unrelated treatments.

### 4.6 WeInivie Membership

**A dark `ink` panel.** Production makes this section a full bleed **orange**
block. That is exactly the large decorative accent fill ch. 2.3 forbids, and the
departure is deliberate: the section that needs the most emphasis on the page
gets it from an ink ground instead. Do not "correct" this back towards
production. Gold is legible as text here, at 6.87 to 1.

**Two columns, not one.** Copy, tagline and CTA on the left; the four benefits
as a two by two grid on the right. A single left column with the benefits strung
along the bottom leaves the top right of the panel empty, which is what makes
the section read as unfinished at 1440.

Production's line is *"Turn Bali Into Yours. Make Every Journey More
Rewarding."*

### 4.7 Our Story

Narrative with four subsections. The natural four are groupings of the eight
mantras, which keeps the section true to the brand rather than generic
about-us copy:

| Subsection | Mantras it carries |
| --- | --- |
| People | Empowering Local People, Intentional Personalization |
| Culture | Respect Local Culture, Meaningful Story Telling |
| Nature | Responsible to Mother Earth, Sustainability Impact |
| Craft | Technology Adaptation, Pioneer Concept |

Production buries Mantras and Sustainability in a secondary menu. Surfacing them
here is deliberate.

### 4.8 Our Special Offers

**Five offers, not four.** One column mobile, two tablet, three desktop with the
first item spanning two columns. Five items fill that grid exactly: a wide one
plus one on the first row, three on the second. Four items leave a visible hole
in the bottom right corner. Production carries five.

Production renders this as a horizontal carousel. It is a grid here, because a
carousel needs script, hides items from the first view, and sits awkwardly
beside the no carousel rule the hero already follows.

**The title appears once.** Production sets it over the photograph and adds no
second label underneath. Follow that, with two corrections: the overlay needs
the same top down scrim the header uses, because white type over an arbitrary
photograph has no guaranteed contrast ratio; and the title is title case, not
all caps. Never render the title over the image and again beneath it.

### 4.9 What's New

Three latest article cards, trimmed from production's six so the page does not
run too long.

### 4.10 Featured In

A row of media logos in greyscale. Production carries nine.

### 4.11 FAQ

An accordion, keyboard operable. Production carries nine question and answer
pairs.

**Centred, following production.** The heading is centred, and the accordion is
a centred column of roughly 900 pixels rather than a left aligned block that
leaves the right half of the container empty. Question text stays left aligned
inside that column, with the expand marker right aligned on the same row. This
is the one section on the page where centring is correct, because a list of
questions has no second column to pair with.

### 4.12 Footer

Four columns desktop, two tablet, one mobile, on an `ink` ground.

**Real** contact data, and deliberately not simplified. Six separate contact
routes is a sincere brand signal, not clutter, and no reference site in ch. 6 has
anything like it.

Column 1, address and brand:

    iNi ViE Hospitality
    Jl. Persada II No.888, Kerobokan, Badung, Bali 80361
    +62 361 9346082
    info@inivie.com

Column 2, departments:

    Reservations      +62 811-3986-889     reservation@inivie.com
    Marketing         +62 812-3868-7387    marcom@inivie.com
    Media Inquiries   +62 813 3753-0285    pr@inivie.com
    Human Resources   +62 812-3729-0110    hire@inivie.com
    Travel Agents                          salescoordinator@inivie.com

Column 3, links:

    About Us · Mantras · Sustainability · Why Choose Us
    Blog · Careers · Owners
    Consultant (DP+, DP Construction)

Column 4, newsletter and social:

    Newsletter signup, one input and one primary button
    Facebook · Instagram · LinkedIn · YouTube · TikTok

Bottom row:

    General Policy
    2026 iNi ViE Hospitality. All Rights Reserved

---

## 4A. Copy rules

**One brand spelling: `iNi ViE`.** Production is inconsistent with itself, using
"Ini Vie" throughout the FAQ and "iNi ViE" everywhere else. This redesign uses
`iNi ViE` in every position, including inside FAQ answers. It is a small
correction worth naming in the README alongside the two production findings in
PRD ch. 2.3.

**Every call to action names its destination.** Production leans on a single
repeated "Discover More", which appeared twelve times on one page in the first
design pass. A control says exactly what happens:

| Where | Label |
| --- | --- |
| Welcome block | About iNi ViE |
| The Culinary Journey | All restaurants |
| Wellness Harmony Escape | All spas |
| Membership, primary | Become a Member |
| Membership, secondary | Membership benefits |
| Our Story, About Us | About us |
| Our Story, What Makes Us Different | Why choose us |
| Our Story, Our Eight Mantras | Read the mantras |
| Our Story, Sustainability | Our sustainability work |

**Em dashes.** Copy quoted verbatim from production keeps whatever punctuation
production used, because it is quoted content. Everything written for this
project uses a plain dash, per the repository convention. Artboard annotations
are written for this project, so they follow the rule.

---

## 5. Responsive rules

| Component | Mobile | Tablet | Desktop |
| --- | --- | --- | --- |
| Property grid | 1 column | 2 columns | 3 columns |
| Culinary and wellness grids | 1 column | 2 columns | 3 columns |
| Special offers | 1 column | 2 columns | 3 columns, first spanning 2 |
| Navigation | Drawer | Drawer | Inline |
| Footer columns | 1 | 2 | 4 |
| Hero height | 70vh | 75vh | 85vh |

Design mobile first. Every rule is a minimum width, never a maximum.

Non negotiable: no horizontal scroll from 320px upward; touch targets at least
44 by 44 pixels on mobile on both axes; navigation collapses to a drawer below
1024px with a focus trap and Escape to close; no clipped or overflowing text on
the longest title in the seed data.

---

## 5A. Motion, hover, and focus

The budget in ch. 2.3 is the whole budget: 200ms, `ease-out`, on hover, focus,
colour and transform. Nothing else. What follows is which states actually have
to be drawn, because a static artboard that declares a transition but never
shows the second state leaves the decision to whoever implements it.

### 5A.1 States that must be drawn

| Element | Rest | Hover | Focus |
| --- | --- | --- | --- |
| Property card | 1px `border`, rest elevation | Raised elevation, image scaled to 1.04 with the overflow clipped | Ring on the card's link |
| Primary button | `accent` fill, `ink` text | `accent-hover` fill, `ink` text | Ring |
| Secondary button | Transparent, 1px `ink` border | Border and label darken | Ring |
| Ghost link | `ink`, no underline | Underline appears | Ring |
| Inert CTA | `muted` on `border` | **No change.** Nothing happens, and nothing suggests it will | Not focusable |
| Nav item | Label only | Label only, no rule | Ring |
| Nav item, active | Accent rule under an `ink` or white label | Unchanged | Ring |
| FAQ row | Collapsed, plus marker | Row ground shifts to `surface-alt` | Ring on the whole row |
| Offer banner | Scrimmed image | Image scaled to 1.04, scrim unchanged | Ring |
| Footer link | `gold` on `ink` | Underline appears | Ring |

**The focus ring is one treatment everywhere**, per ch. 2.3: a two pixel `ink`
outline, offset by two pixels, on `focus-visible` only. `ink` rather than
`accent`, because the ring has to stay legible against the accent fill itself.
Pointer users never see it, keyboard users always do.

### 5A.2 Scroll entrance

A fade plus an upward translate of at most 12 pixels, 200ms, `ease-out`, once
per element. It is not a scroll linked animation and nothing tracks scroll
position continuously. Worth one artboard showing an element mid entrance, so
the 12 pixel limit is a drawn quantity rather than a number in a table.

### 5A.3 Reduced motion is not optional

Every transition and animation collapses under `prefers-reduced-motion: reduce`.
`web/src/app/globals.css` already implements this globally.

**This is a third production weakness not replicated.** 205KB of production CSS
carries 258 hover rules, 51 transitions, 5 keyframe animations and three Swiper
carousels, and **zero** occurrences of `prefers-reduced-motion`. Someone who has
asked their operating system to reduce motion gets the full set anyway.
Vestibular sensitivity is an accessibility concern rather than a preference, so
this belongs alongside the two findings already in PRD ch. 2.3 and in the README
note they feed.

### 5A.4 What production does that this design does not

Production drives Featured Properties, Special Offers and What's New with
Swiper. Three carousels on one homepage means items hidden from the first view,
script on the critical path, and a horizontal drag surface competing with
vertical page scroll on mobile. All three are grids here.

---

## 6. Reference sites

Researched directly on 23 August 2026. **These are references for composition,
not for colour and type.** Ch. 2 is closed.

| Site | Read it for | Do not take |
| --- | --- | --- |
| [comohotels.com](https://www.comohotels.com) | The structural twin. Hotels, COMO Shambhala, COMO Cuisine, Comoclub and Residences map almost one to one onto iNi ViE's six lines. Its nav names intents, not sub-brands: "Wellness", never "COMO Shambhala". Its binding line is *"each one different to the other but sharing the same heart"* | The hero carousel |
| [bawahreserve.com](https://bawahreserve.com) | The best navigation model, and Indonesian. Nav is pure intent: Stay, Taste, Play, Wellbeing, Romance, Gather, Dive. Sustainability is a homepage pillar with concrete proof, not a footer link | All caps headlines |
| [ennismore.com](https://www.ennismore.com) | How to summarise many brands. Dozens of brands compressed into three umbrellas and a six item nav, each brand given two words of character | Its B2B temperature. It has no warmth |
| [aman.com](https://www.aman.com) | Restraint. Whitespace, minimal copy per image, no urgency. The visual translation of *"technology is a tool, not the hero"* | Its emotion. Aman sells distance, iNi ViE sells closeness |
| [nihi.com](https://nihi.com) | The closest warmth, and short poetic headlines that suit a 46px H1 | Its treatment of locality. Sumbanese culture appears through horses and activities rather than through its community, which is exactly what mantras 1, 2 and 6 forbid. Also its sand and cream palette |
| [rosewoodhotels.com](https://www.rosewoodhotels.com) | The philosophical twin. *"A Sense of Place"* is the corporate wording of "Respect Local Culture", executed through local artisans and cultural workshops instead of amenity lists | Its global scale, which thins every destination |

Two more are worth reading and could not be fetched automatically, both
returning HTTP 403. They are unverified and no claim is made about them here:
[desapotatohead.com](https://desapotatohead.com), the closest analogue that
exists, and [banyantreegroup.com](https://www.banyantreegroup.com).

Production itself: [inivie.com](https://inivie.com) and its
[mantras page](https://inivie.com/mantras).

---

## 7. What to avoid

- All caps headlines, including on the offer banners, where production uses them.
- The same generic label on more than one call to action.
- A title rendered twice on one card, over the image and again beneath it.
- Mega menus or nested dropdowns in the header.
- Cream or sand palettes. Colour is closed.
- Orange as a background, a large fill, or a decorative shape, including on the
  membership panel where production uses it.
- A hero carousel, parallax, or scroll jacking.
- A section that fills only the left half of the container at 1440. Either give
  it a second column or centre it, per ch. 4.6 and ch. 4.11.
- Purple to blue gradients, accent rails on rounded cards, emoji as section
  markers, everything centred. These are generator defaults rather than
  decisions.
- Oversized social icons in the footer.
- Placeholder copy. Every string above is real and available.

---

## 8. Acceptance checks

Derived from PRD ch. 9 and DESIGN-SYSTEM ch. 7.3. A design that fails one of
these is not ready to implement.

1. No horizontal scroll at any width from 320px upward.
2. Touch targets at least 44 by 44 pixels on mobile, both axes.
3. Navigation collapses to a drawer below 1024px.
4. Every text and background pairing meets WCAG AA, 4.5 to 1 for body and 3 to 1
   for large text.
5. Accent appears only on buttons, the active nav item, and small markers.
6. Property cards in a row are equal height with the real seed content.
7. The rating omits cleanly when absent, and the price row omits entirely when
   absent, with the CTA inert rather than broken.
8. All six footer departments remain intact and legible at 375px.
9. Verified at 375px, 768px and 1440px.
